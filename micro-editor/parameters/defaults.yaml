# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: micro-editor
  rootgroup: root
  config: '/etc/micro-editor'
  service:
    name: micro-editor
  subcomponent:
    config: '/etc/micro-editor-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
